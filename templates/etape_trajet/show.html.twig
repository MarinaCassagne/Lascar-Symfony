{% extends 'base.html.twig' %}

{% block title %}EtapeTrajet{% endblock %}

{% block body %}
    <h1>EtapeTrajet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape_trajet.id }}</td>
            </tr>
            <tr>
                <th>Latitude_etape</th>
                <td>{{ etape_trajet.latitudeEtape }}</td>
            </tr>
            <tr>
                <th>Longitude_etape</th>
                <td>{{ etape_trajet.longitudeEtape }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etape_trajet_index') }}">back to list</a>

    <a href="{{ path('app_etape_trajet_edit', {'id': etape_trajet.id}) }}">edit</a>

    {{ include('etape_trajet/_delete_form.html.twig') }}
{% endblock %}
